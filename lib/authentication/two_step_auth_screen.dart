import 'package:flutter/material.dart';

class TwoStepAuth extends StatelessWidget {
  const TwoStepAuth({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
