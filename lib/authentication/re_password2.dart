import 'package:flutter/material.dart';

class RePasswordTwo extends StatelessWidget {
  const RePasswordTwo({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
